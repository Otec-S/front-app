import { Box } from "@mui/material";
import { FC } from "react";

const Main: FC = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        bgcolor: "#cfe8fc",
      }}
    >
      <h1>ТЕСТОВАЯ ГЛАВНАЯ СТРАНИЦА</h1>
    </Box>
  );
};

export default Main;
